export interface GalleryImage {
  src: string;
  desc: string;
  category: string;
  title: string;
}

export type GalleryCategory = "All" | "Aircraft" | "Competitions" | "Workshops" | "Team Events";

